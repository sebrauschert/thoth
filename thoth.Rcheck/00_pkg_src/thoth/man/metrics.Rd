% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{metrics}
\alias{metrics}
\title{Calculate model performance metrics}
\usage{
metrics(data, truth, estimate, event_level = NULL, ...)
}
\arguments{
\item{data}{A data frame containing the columns specified in \code{truth} and \code{estimate}.}

\item{truth}{The column name containing the true values.}

\item{estimate}{The column name containing the predicted values.}

\item{event_level}{A character string indicating which level of the outcome is considered the "event".}

\item{...}{Additional arguments passed to yardstick::metrics.}
}
\value{
A tibble with model performance metrics.
}
\description{
Calculate model performance metrics
}
\examples{
\dontrun{
library(dplyr)
data(mtcars)
# Create a binary outcome
mtcars <- mtcars \%>\% 
  mutate(vs_factor = factor(vs))
# Fit a model
model <- glm(vs ~ mpg + cyl, data = mtcars, family = "binomial")
# Make predictions
preds <- predict(model, type = "response")
# Create prediction data frame
pred_data <- mtcars \%>\%
  mutate(pred = factor(ifelse(preds > 0.5, 1, 0)))
# Calculate metrics
metrics(pred_data, truth = vs_factor, estimate = pred)
}
}
